<Window x:Class="HotelManagement.GUI.Dialogs.ChiTietLuuTruDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Chi tiết lưu trú"
        Height="650"
        Width="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5FBFB">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="220"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="CHI TIẾT LƯU TRÚ"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#0F4C5C"
                   Margin="0,0,0,20"/>

        <!-- Thông tin -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Mã đặt phòng:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtMaDatPhong" Grid.Column="1" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="Phòng:" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSoPhong" Grid.Column="3" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="Khách hàng:" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox x:Name="txtKhachHang" Grid.Row="1" Grid.Column="1" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="CCCD:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtCCCD" Grid.Row="1" Grid.Column="3" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="SĐT:" Grid.Row="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSDT" Grid.Row="2" Grid.Column="1" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="Loại phòng:" Grid.Row="2" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtLoaiPhong" Grid.Row="2" Grid.Column="3" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="Thời gian nhận:" Grid.Row="3" VerticalAlignment="Center"/>
                <TextBox x:Name="txtThoiGianNhan" Grid.Row="3" Grid.Column="1" IsReadOnly="True" Margin="5"/>

                <TextBlock Text="Ngày trả dự kiến:" Grid.Row="3" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtNgayTraDuKien" Grid.Row="3" Grid.Column="3" IsReadOnly="True" Margin="5"/>
            </Grid>
        </Border>

        <!-- tiêu đề dịch vụ -->
        <TextBlock Grid.Row="2"
                   Text="DANH SÁCH DỊCH VỤ ĐÃ DÙNG"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,20,0,10"
                   Foreground="#0F4C5C"/>

        <!-- bảng dịch vụ -->
        <Border Grid.Row="3"
                Background="White"
                CornerRadius="10"
                Padding="10">
            <DataGrid x:Name="dgDichVu"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã CTDV" Binding="{Binding MaCTDV}" Width="100"/>
                    <DataGridTextColumn Header="Tên dịch vụ" Binding="{Binding TenDichVu}" Width="200"/>
                    <DataGridTextColumn Header="Loại dịch vụ" Binding="{Binding LoaiDichVu}" Width="150"/>
                    <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong}" Width="100"/>
                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- nút -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button x:Name="btnThemDichVu"
                    Content="Thêm dịch vụ"
                    Width="140"
                    Margin="10,0"/>

            <Button x:Name="btnThanhToan"
                    Content="Thanh toán"
                    Width="140"
                    Margin="10,0"/>

            <Button x:Name="btnDong"
                    Content="Đóng"
                    Width="140"
                    Margin="10,0"
                    Background="#9E9E9E"/>
        </StackPanel>
    </Grid>
</Window>