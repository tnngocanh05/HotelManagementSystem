<Window x:Class="HotelManagement.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hotel Management"
        Height="820"
        Width="1450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <StackPanel>
                <TextBlock Text="Hotel Management"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="20,20,20,30"/>

                <Button x:Name="btnTrangChu" Content="Trang chủ"/>
                <Button x:Name="btnLoaiPhong" Content=" Loại Phòng"/>
                <Button x:Name="btnPhong" Content="Quản lý phòng"/>
                <Button x:Name="btnDichVu" Content="Dịch Vụ"/>
                <Button x:Name="btnKhachHang" Content="Khách Hàng"/>
                <Button x:Name="btnDanhSachDatPhong" Content="Danh sách đặt phòng"/>
                <Button x:Name="btnNhanVien" Content="Nhân viên"/>
                <Button x:Name="btnHoaDon" Content="Hoá đơn"/>
                <Button x:Name="btnThongKe" Content="Thống kê"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource HeaderBrush}"
                    Margin="10,10,10,5"
                    CornerRadius="8">
                <Grid>
                    <TextBlock Text="HỆ THỐNG QUẢN LÝ KHÁCH SẠN"
                               Foreground="White"
                               FontSize="24"
                               FontWeight="Bold"
                               VerticalAlignment="Center"
                               Margin="20,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Background="White"
                    Margin="10,5,10,10"
                    CornerRadius="8">
                <Frame x:Name="MainFrame"
                       NavigationUIVisibility="Hidden"
                       Background="Transparent" />
            </Border>
        </Grid>
    </Grid>
</Window>
