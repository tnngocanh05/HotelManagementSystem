<UserControl x:Class="HotelManagement.GUI.Views.PhongView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Thêm phòng mới" Click="BtnThem_Click" Width="130" Height="35" Background="#2ecc71" Foreground="White"/>
        </StackPanel>

        <DataGrid x:Name="dgPhong" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã" Binding="{Binding MaPhong}" Width="60"/>
                <DataGridTextColumn Header="Tên Phòng" Binding="{Binding SoPhong}" Width="*"/>
                <DataGridTextColumn Header="Loại Phòng" Binding="{Binding TenLoaiPhong}" Width="*"/>
                <DataGridTextColumn Header="Tình Trạng" Binding="{Binding TinhTrang}" Width="120"/>

                <DataGridTemplateColumn Header="Hành động" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Sửa" Click="BtnSua_Click" 
                                        Background="#3498db" Foreground="White" 
                                        Padding="10,2" Margin="0,0,5,0" BorderThickness="0"/>

                                <Button Content="Xóa" Click="BtnXoa_Click" 
                                        Background="#e74c3c" Foreground="White" 
                                        Padding="10,2" Margin="0,0,5,0" BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>