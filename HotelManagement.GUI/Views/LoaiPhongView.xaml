<UserControl x:Class="HotelManagement.GUI.Views.LoaiPhongView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="+ Thêm loại phòng mới" 
                Click="BtnThem_Click" 
                HorizontalAlignment="Left" 
                Padding="15,7" 
                Background="#2ecc71" 
                Foreground="White" 
                FontWeight="Bold" 
                Cursor="Hand"/>

        <DataGrid x:Name="dgv" 
                  Grid.Row="1" 
                  Margin="0,10,0,0" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  GridLinesVisibility="Horizontal" 
                  HeadersVisibility="Column"
                  Background="Transparent"
                  BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã" Binding="{Binding MaLoaiPhong}" Width="60"/>
                <DataGridTextColumn Header="Tên Loại Phòng" Binding="{Binding TenLoaiPhong}" Width="*"/>
                <DataGridTextColumn Header="Số người" Binding="{Binding SoNguoiToiDa}" Width="100"/>
                <DataGridTextColumn Header="Giá phòng" Binding="{Binding GiaTien, StringFormat={}{0:N0}đ}" Width="150"/>

                <DataGridTemplateColumn Header="Hành động" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Sửa" 
                                        Click="BtnSua_Click" 
                                        Padding="10,2" 
                                        Margin="0,0,5,0"
                                        Background="#3498db" 
                                        Foreground="White"/>

                                <Button Content="Xóa" 
                                        Click="BtnXoa_Click" 
                                        Padding="10,2" 
                                        Background="#e74c3c" 
                                        Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>