<UserControl x:Class="HotelManagement.GUI.Views.TrangChuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1200">

    <Grid Margin="15" Background="#F5FBFB">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- BỘ LỌC -->
        <Border Grid.Column="0"
                Background="#DFF3F3"
                CornerRadius="10"
                Padding="15"
                Margin="0,0,10,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="TÌM PHÒNG"
                               FontSize="22"
                               FontWeight="Bold"
                               Margin="0,0,0,15"/>

                    <TextBlock Text="Mã phòng"/>
                    <TextBox x:Name="txtSearchRoom"/>

                    <TextBlock Text="Ngày nhận"/>
                    <DatePicker x:Name="dpNgayNhan"/>

                    <TextBlock Text="Giờ nhận"/>
                    <ComboBox x:Name="cbGioNhan"/>

                    <TextBlock Text="Số đêm"/>
                    <ComboBox x:Name="cbSoDem"/>

                    <TextBlock Text="Loại phòng"/>
                    <ComboBox x:Name="cbLoaiPhong"/>

                    <TextBlock Text="Tầng"/>
                    <ComboBox x:Name="cbTang"/>

                    <Button x:Name="btnTimPhong"
                            Content="Tìm phòng trống"
                            Margin="0,15,0,10"/>

                    <TextBlock Text="CHÚ THÍCH"
                               FontWeight="Bold"
                               Margin="0,20,0,10"/>

                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Border Width="18" Height="18" Background="#BEE9FF" Margin="0,0,8,0"/>
                        <TextBlock Text="Phòng trống" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Border Width="18" Height="18" Background="#4CAF50" Margin="0,0,8,0"/>
                        <TextBlock Text="Đang ở / Đã đặt" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Border Width="18" Height="18" Background="#9E9E9E" Margin="0,0,8,0"/>
                        <TextBlock Text="Bảo trì" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Border Width="18" Height="18" Background="#F4C542" Margin="0,0,8,0"/>
                        <TextBlock Text="Dọn dẹp" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- DANH SÁCH PHÒNG -->
        <Border Grid.Column="1"
                Background="White"
                CornerRadius="10"
                Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="RoomContainer"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>